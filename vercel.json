{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @tekstil/db exec prisma generate && pnpm --filter @tekstil/db exec prisma migrate deploy && pnpm --filter @tekstil/web build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
